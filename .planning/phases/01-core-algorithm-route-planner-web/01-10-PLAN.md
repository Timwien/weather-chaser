---
phase: 01-core-algorithm-route-planner-web
plan: 10
type: execute
wave: 7
depends_on:
  - "01-08"
  - "01-09"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can complete the full flow from empty state to shared result without errors"
    - "All 5 phase success criteria are verifiable end-to-end"
    - "No hardcoded user-facing strings found in any component"
    - "pnpm turbo build exits 0 for the entire monorepo"
    - "All packages/core tests pass"
---

<objective>
End-to-end verification checkpoint for Phase 1. No code changes — purely human verification of all 5 phase success criteria against the running application.

Purpose: Phase 1 is not complete until a human has verified the full flow works as specified. This checkpoint is the gate before marking Phase 1 done.
Output: Human confirmation that all success criteria pass, or a list of specific issues to address via gap closure.
</objective>

<execution_context>
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification build and test check</name>
  <files></files>
  <action>
    Run the following checks from the repo root and report results:

    1. `pnpm turbo build` — exits 0, all packages compile
    2. `pnpm turbo type-check` — exits 0, no TypeScript errors across the monorepo
    3. `cd packages/core && pnpm test` — all tests pass (scoring + optimizer TDD suites)
    4. Verify no hardcoded user-facing strings:
       ```
       grep -r --include="*.tsx" --include="*.ts" \
         -E ">[A-Z][a-z]+ [a-z]|>[A-Z][a-z]+ [A-Z]" \
         apps/web/src/components/ | grep -v "t('"
       ```
       Any matches are candidates for missing i18n keys (review each hit — some may be technical strings, not user-facing).

    Report all results. If any check fails, fix before proceeding to the human checkpoint.
  </action>
  <verify>
    All four checks exit 0 / produce no unexpected output.
  </verify>
  <done>
    turbo build passes; turbo type-check passes; packages/core tests pass; no obvious hardcoded user-facing strings
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end Phase 1 verification against all 5 success criteria</name>
  <action>Human verification of all 5 phase success criteria — no code changes required. See how-to-verify steps below.</action>
  <what-built>
    The complete Phase 1 application: Turborepo monorepo, shared TypeScript algorithm library, Vite + React web app, full-screen MapLibre map, floating entry panel, route optimization pipeline, day-by-day itinerary, score-colored map markers, per-segment route line, and share/export features.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` from the repo root (or `turbo dev --filter=@weatherchaser/web`) and open http://localhost:5173.

    **Success Criterion 1:** Search area + route generation through named real towns
    - Type "Black Forest" (Schwarzwald) in the location field
    - Set a 7-day date range starting from today
    - Click "Find Best Route"
    - Set start location to "Freiburg im Breisgau"
    - Set max stay to 2 nights
    - Click "Generate Route"
    - PASS if: the result shows real town names (not coordinates or grid points), the loading steps appear in sequence, and a route is generated

    **Success Criterion 2:** Day-by-day itinerary with temporal scores
    - After generation: itinerary panel shows specific dates per stop (e.g. "Mon 10 Jul")
    - Each stop shows a weather score
    - PASS if: scores differ between stops (temporal — each stop scored for the days the user is there, not a uniform average)

    **Success Criterion 3:** Geographic progression on map
    - On the map: numbered markers appear at stop locations
    - Route line connects them in order
    - PASS if: the route roughly follows geographic progression (no obvious criss-crossing), and the number on the first marker matches the "Day 1" stop in the itinerary

    **Success Criterion 4:** Export and shareable link
    - Click "Open in Google Maps" — Google Maps opens in a new tab with the route loaded
    - Click "Copy shareable link" — "Link copied" feedback appears
    - Open a new browser tab, paste the link — the full results view restores (stops, scores, map markers)
    - PASS if: both exports work and the shared link restores results for someone with no account

    **Success Criterion 5:** Must-visit stop
    - Go back to the entry panel (reload the page)
    - Enter a region, set dates, click Find Best Route
    - In the must-visit input, add "Munich" (or another town in the region)
    - Generate the route
    - PASS if: "Munich" (or the entered town) appears as one of the stops in the itinerary

    **Additional checks:**
    - The design looks premium and clean — no emoji in the UI, no gradients except score bars
    - The map never goes blank during route generation
    - On a mobile-sized viewport (use browser devtools to set 375px width): the itinerary occupies the full screen (NOT a bottom sheet); a tab bar with "Itinerary" and "Map" tabs appears at the bottom; tapping "Map" switches to full-screen map; tapping "Itinerary" returns to the itinerary

    Report: For each of the 5 criteria, write PASS or FAIL + observations.
  </how-to-verify>
  <resume-signal>Report results: "All 5 pass" or list failures with details for gap closure</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria verified by a human:
1. Real named towns in route (not grid points)
2. Temporal scores — different scores per stop based on specific stay dates
3. Geographic progression — no obvious criss-crossing
4. Google Maps export works; shareable link restores results
5. Must-visit town appears in generated route
</verification>

<success_criteria>
Human reports all 5 phase success criteria as PASS.
If any criterion fails: create gap closure plans via `/gsd:plan-phase --gaps`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-algorithm-route-planner-web/01-10-SUMMARY.md`

If all criteria pass, update STATE.md:
- Phase 1 status: Complete
- Progress: 100%
- Last activity: today's date
</output>
