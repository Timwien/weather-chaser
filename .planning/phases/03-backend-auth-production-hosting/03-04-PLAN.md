---
phase: 03-backend-auth-production-hosting
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - apps/web/src/components/account/AccountModal.tsx
  - apps/web/src/components/account/AccountModal.css
  - apps/web/src/components/account/AccountTab.tsx
  - apps/web/src/components/account/SavedTab.tsx
  - apps/web/src/components/account/SettingsTab.tsx
  - apps/web/src/components/auth/InlineSignInPrompt.tsx
  - apps/web/src/components/auth/InlineSignInPrompt.css
  - apps/web/src/components/entry/EntryPanel.tsx
  - apps/web/src/components/entry/EntryPanel.css
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Small account/settings icon appears at the bottom of the entry panel"
    - "Tapping the icon opens a full-screen modal with 3 tabs: Account | Saved | Settings"
    - "Guest users see sign-in options (email, Google, Apple) in the Account tab"
    - "Logged-in users see their email/name in the Account tab with a Sign Out button"
    - "Modal closes after successful sign-in without disrupting the current route or map state"
    - "InlineSignInPrompt renders compact sign-in buttons (Google, Apple, Email) with the message 'Anmelden zum Speichern'"
  artifacts:
    - path: "apps/web/src/components/account/AccountModal.tsx"
      provides: "Full-screen modal with Account/Saved/Settings tabs"
      exports: ["AccountModal"]
    - path: "apps/web/src/components/account/AccountTab.tsx"
      provides: "Sign-in forms or logged-in user profile view"
      exports: ["AccountTab"]
    - path: "apps/web/src/components/account/SavedTab.tsx"
      provides: "Empty state placeholder (populated in Plan 03-05)"
      exports: ["SavedTab"]
    - path: "apps/web/src/components/account/SettingsTab.tsx"
      provides: "Settings tab with delete account option and privacy/ToS links"
      exports: ["SettingsTab"]
    - path: "apps/web/src/components/auth/InlineSignInPrompt.tsx"
      provides: "Compact inline sign-in prompt for gated actions"
      exports: ["InlineSignInPrompt"]
    - path: "apps/web/src/components/entry/EntryPanel.tsx"
      provides: "Account icon at panel footer + modal trigger"
      contains: "AccountModal"
  key_links:
    - from: "apps/web/src/components/entry/EntryPanel.tsx"
      to: "apps/web/src/components/account/AccountModal.tsx"
      via: "useState for isAccountModalOpen"
      pattern: "AccountModal"
    - from: "apps/web/src/components/account/AccountTab.tsx"
      to: "apps/web/src/stores/authStore.ts"
      via: "useAuthStore for sign-in methods and user state"
      pattern: "useAuthStore"
    - from: "apps/web/src/components/auth/InlineSignInPrompt.tsx"
      to: "apps/web/src/stores/authStore.ts"
      via: "signInWithGoogle, signInWithApple, signInWithEmail"
      pattern: "useAuthStore"
---

<objective>
Build the account modal (3 tabs: Account | Saved | Settings), the inline sign-in prompt for gated actions, and add the account/settings icon to the EntryPanel footer per the locked user decisions.

Purpose: Auth UI entry point. All three sign-in methods (email/password, Google OAuth, Apple OAuth) must work within the modal. After sign-in, modal closes seamlessly without disrupting the current route state.

Output: AccountModal + 3 tab components + InlineSignInPrompt + EntryPanel footer icon. SavedTab is a placeholder here — populated in Plan 03-05.
</objective>

<execution_context>
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/stores/authStore.ts
@apps/web/src/components/entry/EntryPanel.tsx
@apps/web/src/components/entry/EntryPanel.css
@.planning/phases/03-backend-auth-production-hosting/03-CONTEXT.md
@.planning/phases/03-backend-auth-production-hosting/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountModal with 3 tabs and AccountTab sign-in forms</name>
  <files>apps/web/src/components/account/AccountModal.tsx
apps/web/src/components/account/AccountModal.css
apps/web/src/components/account/AccountTab.tsx
apps/web/src/components/account/SavedTab.tsx
apps/web/src/components/account/SettingsTab.tsx</files>
  <action>
Create the account modal directory and components. Style to match the existing app aesthetic (petrol teal #0E7490 accent, clean sans-serif, consistent with EntryPanel and ItineraryPanel style).

`apps/web/src/components/account/AccountModal.tsx`:
- Full-screen overlay (`position: fixed; inset: 0; z-index: 1000; background: rgba(0,0,0,0.5)`)
- Inner panel: centered card, max-width 420px, white background, border-radius 12px, padding 24px
- Tab bar at top: 3 tabs "Konto | Gespeichert | Einstellungen" (German, matching app language)
- Active tab underlined with #0E7490
- Renders AccountTab, SavedTab, or SettingsTab based on active tab state
- Close button (×) top-right
- Props: `isOpen: boolean; onClose: () => void; initialTab?: 'account' | 'saved' | 'settings'`
- After successful sign-in: call `onClose()` — detected via `useEffect` watching `user` state change from null to truthy

`apps/web/src/components/account/AccountTab.tsx`:
- If `user` is null (guest): show sign-in UI
  - Header: "Willkommen bei WeatherChaser"
  - Email input + password input + "Anmelden" button (calls `signInWithEmail`)
  - "Noch kein Konto? Registrieren" toggle to switch to sign-up mode (same form, calls `signUpWithEmail`)
  - Divider "oder"
  - Google button: white button with Google icon text "Mit Google anmelden"
  - Apple button: black button with Apple icon text "Mit Apple anmelden"
  - Loading state during async calls: disable buttons, show spinner
  - Error display: red text below form for thrown errors
- If `user` is set (logged in): show profile view
  - User's email address (or display name if set)
  - "Abmelden" button (calls `signOut`)
- Use `useAuthStore` for all auth actions and user state

`apps/web/src/components/account/SavedTab.tsx`:
- Placeholder for Plan 03-05. Show empty state:
  - Icon (bookmark or cloud icon as SVG)
  - "Noch nichts gespeichert" heading
  - "Speichere Routen und Favoriten, um sie hier zu sehen."
- Export `SavedTab` component (no props)

`apps/web/src/components/account/SettingsTab.tsx`:
- "Datenschutz" heading
- Privacy Policy link: `<a href="/privacy" target="_blank">Datenschutzerklärung</a>` (placeholder URL — actual page not in scope for Phase 3; link is sufficient per GDPR minimum)
- Terms of Service link: `<a href="/tos" target="_blank">Nutzungsbedingungen</a>`
- Divider
- "Konto löschen" section:
  - Explanation: "Dein Konto und alle gespeicherten Daten werden dauerhaft gelöscht."
  - Button: "Konto löschen" (red/destructive styling)
  - On click: show confirmation dialog (`window.confirm` is acceptable for MVP)
  - On confirm: call `DELETE /api/user/delete` with `Authorization: Bearer <session.access_token>` header; on success, call `signOut()` and `onClose()`
  - On error: display error message
- Only show "Konto löschen" if user is logged in; if not logged in, show "Anmelden, um Einstellungen zu verwalten"

Create `apps/web/src/components/account/AccountModal.css` with all styles for the modal, tabs, and form elements. Follow the CSS variable and class naming patterns from EntryPanel.css.
  </action>
  <verify>Run `pnpm --filter @weatherchaser/web type-check`. All new files compile without errors.</verify>
  <done>AccountModal renders a full-screen overlay with 3 tab components. AccountTab shows sign-in form (email + Google + Apple) for guests, profile view for logged-in users. SavedTab shows empty state placeholder. SettingsTab shows privacy links and delete account button. CSS written. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: InlineSignInPrompt and EntryPanel account icon</name>
  <files>apps/web/src/components/auth/InlineSignInPrompt.tsx
apps/web/src/components/auth/InlineSignInPrompt.css
apps/web/src/components/entry/EntryPanel.tsx
apps/web/src/components/entry/EntryPanel.css</files>
  <action>
Create `apps/web/src/components/auth/InlineSignInPrompt.tsx`:
- Small inline component (not full-screen) for when a guest taps a gated action
- Layout: narrow card with close (×) button
- Header line: "Anmelden zum Speichern — kostenlos"
- Row of 3 compact buttons: Google | Apple | E-Mail
  - Google button: calls `signInWithGoogle()` from authStore
  - Apple button: calls `signInWithApple()` from authStore
  - E-Mail button: shows a minimal inline form (email + password fields) inline below the buttons; same signInWithEmail/signUpWithEmail as AccountTab
- Props: `onClose: () => void`
- After sign-in detected (user goes from null to truthy via useEffect): call `onClose()` — the calling component then completes the pending action
- Error display: small red text below buttons

Create `apps/web/src/components/auth/InlineSignInPrompt.css` with compact styling.

Update `apps/web/src/components/entry/EntryPanel.tsx`:
- Add an account icon at the very bottom of the `<aside className="entry-panel">` element — OUTSIDE the existing content sections, as a footer row
- Icon: use a simple user/person SVG icon (inline SVG, 20px, petrol teal color)
- Clicking icon: set `isAccountModalOpen` state to true, render `<AccountModal>`
- Add `<AccountModal isOpen={isAccountModalOpen} onClose={() => setIsAccountModalOpen(false)} />` near the bottom of the JSX
- For logged-in users: show a small avatar circle with user initials (first letter of email) next to the icon so the user can see they're signed in
- Import and use `useAuthStore` to read `user` state for the avatar display

Update `apps/web/src/components/entry/EntryPanel.css`:
- Add `.entry-panel-footer` styles: fixed to bottom of panel, flex row with icon button
- Icon button: no background, border: none, cursor: pointer, padding: 8px
- Avatar circle: 28px circle, #0E7490 background, white text, 12px font-size
  </action>
  <verify>Run `pnpm --filter @weatherchaser/web type-check`. Run `pnpm dev`, open app — account icon visible at bottom of entry panel. Click icon — full-screen modal opens with 3 tabs. Modal closes via × button.</verify>
  <done>Account icon visible at EntryPanel bottom. Clicking opens AccountModal. All 3 tabs render. InlineSignInPrompt exists and exports correctly. Type-check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth UI in browser</name>
  <what-built>
    Account modal (3 tabs: Konto | Gespeichert | Einstellungen), account icon in EntryPanel footer, InlineSignInPrompt component. Email sign-in form, Google OAuth button, Apple OAuth button.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and open http://localhost:5173

    1. Look at the bottom of the left entry panel — confirm a small person/user icon is visible
    2. Click the icon — confirm a full-screen modal overlay appears
    3. Confirm 3 tabs: "Konto", "Gespeichert", "Einstellungen" (or German equivalents)
    4. In Konto tab: confirm email input, password input, Anmelden button, Google button, Apple button
    5. Click "Gespeichert" tab — confirm empty state message with no errors
    6. Click "Einstellungen" tab — confirm Datenschutzerklärung link, Nutzungsbedingungen link visible
    7. Test email sign-up: enter email + password in AccountTab, click register button — confirm Supabase sends confirmation email (check inbox; Supabase sends from noreply@mail.app.supabase.io)
    8. Close modal with × — confirm map/route state is completely undisturbed
    9. After confirming email (from email link), sign in — confirm user email appears in Account tab and modal closes
    10. Sign out — confirm Account tab reverts to sign-in form
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which steps failed and what you saw</resume-signal>
  <action>Human checkpoint — see how-to-verify instructions above.</action>
  <verify>Human has completed verification steps and typed resume signal.</verify>
  <done>Human has approved or described issues via resume-signal.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @weatherchaser/web type-check` exits 0
2. Account icon visible at bottom of EntryPanel in browser
3. AccountModal opens/closes without disrupting map or route state
4. All 3 tabs render without console errors
5. Email sign-up triggers Supabase confirmation email
6. After confirming email + signing in: user email visible in Account tab, Sign Out button present
</verification>

<success_criteria>
Account/settings icon at EntryPanel bottom. Full-screen modal with 3 tabs. Email+Google+Apple sign-in in Account tab. Guest users see sign-in form; logged-in users see profile. Modal closes without disrupting app state. Human verification passed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-auth-production-hosting/03-04-SUMMARY.md`
</output>
