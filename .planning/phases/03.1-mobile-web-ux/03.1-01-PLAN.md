---
phase: 03.1-mobile-web-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/hooks/useIsMobile.ts
  - apps/web/src/components/mobile/MobileBottomSheet.tsx
  - apps/web/src/components/mobile/MobileBottomSheet.css
autonomous: true
requirements:
  - UI-MOBILE-SHEET-01
  - UI-MOBILE-BREAKPOINT-01

must_haves:
  truths:
    - "useIsMobile() returns true at viewport width ≤768px and false above, and updates live on resize/orientation change without a reload"
    - "The bottom sheet has three snap detents — peek (handle + summary strip only), half (~50% height), full (~100dvh) — and the user can drag the handle between them"
    - "Dragging the handle moves the sheet at 60fps (transform driven by ref during drag, React state updated only on release); releasing snaps to the nearest detent with a 200ms ease-out transition"
    - "A scrim dims and intercepts taps over the map ONLY at the full detent; at peek/half the map stays bright and the scrim does not block pointer events"
    - "The sheet and its handle/summary/scrim use only design tokens (var(--glass-*), var(--color-*), var(--shadow-*)) so they adapt to [data-theme=dark] with no per-component dark styles"
    - "The sheet content area scrolls independently (overflow-y:auto, overscroll-behavior:contain) and the drag listener is bound only to the handle, so list scrolling never fights the drag-to-snap gesture"
  artifacts:
    - path: "apps/web/src/hooks/useIsMobile.ts"
      provides: "SSR-safe matchMedia('(max-width: 768px)') boolean hook with change listener"
      exports: ["useIsMobile"]
    - path: "apps/web/src/components/mobile/MobileBottomSheet.tsx"
      provides: "Three-snap draggable bottom sheet (controlled snapIndex + onSnapChange), handle, peek summary slot, scrim at full only, scrollable content area"
      exports: ["MobileBottomSheet"]
      min_lines: 80
    - path: "apps/web/src/components/mobile/MobileBottomSheet.css"
      provides: "Sheet positioning, snap transforms, handle, scrim, 100dvh full height with safe-area fallback, token-driven glass styling"
      contains: "dvh"
  key_links:
    - from: "apps/web/src/components/mobile/MobileBottomSheet.tsx"
      to: "the handle pointer events"
      via: "onPointerDown/Move/Up with setPointerCapture; transform via ref during drag, setState only on release"
      pattern: "setPointerCapture"
    - from: "apps/web/src/components/mobile/MobileBottomSheet.tsx"
      to: "onSnapChange callback"
      via: "fires the new snap index (0|1|2) on pointerup after nearest-detent computation"
      pattern: "onSnapChange"
---

<objective>
Build the two foundation primitives the rest of Phase 3.1 composes on top of: a `useIsMobile()` breakpoint hook and a custom three-snap draggable `MobileBottomSheet`. Per RESEARCH, the sheet is built custom (~80 lines pointer-events + CSS transitions) rather than pulling in `vaul` (unmaintained) or `react-modal-sheet` (adds the ~34 kB `motion` peer dep with unresolved React 19 compat). The project has zero animation-library dependency today and this plan keeps it that way.

This plan ships the primitives only — it does NOT wire them into `routes/index.tsx` yet (that is Plan 03). The sheet is a controlled component (`snapIndex` + `onSnapChange`) so the composition plan owns the snap state and can convert it to MapLibre padding.

Purpose: a reusable, dependency-free, token-themed sheet that behaves like the Apple/Google-Maps bottom sheet from the locked CONTEXT decisions (peek/half/full, drag handle, peek summary, light scrim at full only).
Output: `useIsMobile.ts`, `MobileBottomSheet.tsx`, `MobileBottomSheet.css`.

Out of scope: search bar (Plan 02), index.tsx composition / CSS cleanup / map padding (Plan 03).
</objective>

<execution_context>
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-mobile-web-ux/03.1-CONTEXT.md
@.planning/phases/03.1-mobile-web-ux/03.1-RESEARCH.md
@apps/web/src/styles/tokens.css
@apps/web/src/stores/themeStore.ts
@apps/web/src/components/entry/EntryPanel.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: useIsMobile hook</name>
  <files>apps/web/src/hooks/useIsMobile.ts</files>
  <action>
Create `apps/web/src/hooks/useIsMobile.ts`. SSR-safe `matchMedia('(max-width: 768px)')` boolean hook — same pattern `themeStore.ts` already uses for `prefers-color-scheme`. 768px is the locked breakpoint (matches every existing `@media (max-width: 768px)` rule in the CSS).

```typescript
import { useEffect, useState } from 'react';

const QUERY = '(max-width: 768px)';

function read(): boolean {
  return typeof window !== 'undefined' && window.matchMedia(QUERY).matches;
}

/** True on phone/portrait-tablet viewports (≤768px). Updates live on resize/orientation change. */
export function useIsMobile(): boolean {
  const [isMobile, setIsMobile] = useState<boolean>(read);

  useEffect(() => {
    const mq = window.matchMedia(QUERY);
    const onChange = () => setIsMobile(mq.matches);
    mq.addEventListener('change', onChange);
    // sync once in case the media state changed between SSR/first render and effect
    setIsMobile(mq.matches);
    return () => mq.removeEventListener('change', onChange);
  }, []);

  return isMobile;
}
```

No new dependency. No hardcoded strings (none are user-facing here).
  </action>
  <verify>`pnpm --filter @weatherchaser/web type-check` exits 0. `grep -n "max-width: 768px" apps/web/src/hooks/useIsMobile.ts` shows the query string matching the existing CSS breakpoint.</verify>
  <done>useIsMobile.ts exports a `useIsMobile()` hook returning a boolean, SSR-safe (guards `typeof window`), with a `matchMedia` change listener cleaned up on unmount. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: MobileBottomSheet primitive (3-snap drag) + CSS</name>
  <files>apps/web/src/components/mobile/MobileBottomSheet.tsx
apps/web/src/components/mobile/MobileBottomSheet.css</files>
  <action>
Create the custom three-snap draggable sheet. Controlled component: parent owns `snapIndex` (0=peek, 1=half, 2=full) and passes `onSnapChange`.

**Component contract (`MobileBottomSheet.tsx`):**
```typescript
interface MobileBottomSheetProps {
  snapIndex: 0 | 1 | 2;                 // controlled: 0 peek, 1 half, 2 full
  onSnapChange: (index: 0 | 1 | 2) => void;
  summary?: React.ReactNode;            // peek strip content (e.g. "6 Orte gefunden") — provided by Plan 03
  children: React.ReactNode;            // scrollable sheet content (the panels)
}
```

**Snap mechanics (follow RESEARCH Pattern 2 + anti-patterns exactly):**
- Compute the three snap top-offsets from `window.innerHeight`: `peek` leaves only ~84px visible (handle + summary strip); `half` ≈ 50dvh; `full` ≈ top safe-area inset. Store snap *translateY* values (sheet is full-height, translated down to reveal less).
- Outer fixed wrapper: `position: fixed; left:0; right:0; bottom:0; height: 100dvh` with the CSS transform applied. Apply `transition: transform 200ms ease-out` ONLY when not actively dragging (toggle a `dragging` ref / data-attribute that removes the transition class during drag).
- Drag listener is bound ONLY to the handle `<div>` (NOT the whole sheet). Handle has `touch-action: none`. On `pointerdown`: `handleEl.setPointerCapture(e.pointerId)`, record start Y + current translateY, set `dragging`. On `pointermove`: write `sheetEl.style.transform = translateY(clamped)` directly via ref — NO React state update (prevents re-render thrash → 60fps). On `pointerup`/`pointercancel`: compute nearest snap from the released translateY, clear `dragging`, call `onSnapChange(nearestIndex)`, release pointer capture. The committed `snapIndex` prop drives the transform via `useEffect`/style so the controlled value and the visual stay in sync after release.
- Recompute snap pixel values on resize/orientation (listen to `resize`); keep current snapIndex.

**Handle + summary:** A grab handle pill at the top (`var(--color-border)` capsule, ~36×4px, centered) inside a header region. Directly under it render `{summary}` (the peek strip). Tapping the handle should also cycle: a plain `onClick` on the handle that advances peek→half→full (and full taps do nothing / stay) — RESEARCH says "tapping the handle also works". Distinguish tap from drag by a small movement threshold (e.g. if total drag < 6px treat pointerup as a tap → advance one detent).

**Scrim:** A sibling element inside the sheet's fixed container (`position: fixed; inset: 0`) at z-index just below the sheet body. Render/fade it in ONLY when `snapIndex === 2`. `pointer-events: none` at peek/half (map stays interactive); `pointer-events: auto` at full so a backdrop tap calls `onSnapChange(1)` (collapse to half). Background `rgba(0,0,0,0.25)` — a fixed value is acceptable per RESEARCH (scrim behavior doesn't change in dark mode); transition opacity 200ms.

**Content area:** A child wrapper with `overflow-y: auto; overscroll-behavior: contain; flex: 1` holding `{children}`. The OUTER sheet wrapper must be `overflow: visible` (RESEARCH Pitfall 6 — the DateRangePicker `drp-popover` is `position:absolute;z-index:100` and would be clipped); clip the rounded top corners with `border-radius` on the visible body, not `overflow:hidden` on the wrapper.

**CSS (`MobileBottomSheet.css`) — token-driven only, no hardcoded colors except the scrim rgba:**
- Sheet body background `var(--glass-bg)`, `backdrop-filter: var(--glass-blur)`, `border-top: var(--glass-border)`, `box-shadow: var(--shadow-glass)`, top corners `border-radius: var(--radius-xl) var(--radius-xl) 0 0`.
- Full-height uses `height: 100dvh` with fallback `height: calc(100vh - env(safe-area-inset-bottom))` (RESEARCH Pitfall 5 — iOS Safari 100vh).
- `z-index: 40` on the sheet (per RESEARCH z-index stack; search bar will be 50, loading 200).
- Handle pill: `var(--color-border)`, `border-radius: var(--radius-full)`, `cursor: grab`, `touch-action: none`.
- Optional landscape guard (RESEARCH Open Question 1): `@media (orientation: landscape) and (max-height: 500px)` caps the half detent at ~70% height — implementer's call; include if low-cost.

i18n: this primitive renders NO literal user-facing strings of its own (summary text is passed in by Plan 03). Do not hardcode any. Any aria-label you add (e.g. on the handle) MUST be a prop or a t() call — but prefer leaving labels to Plan 03 which has the i18n keys; if you add a handle aria-label here, accept it as an optional prop rather than hardcoding.
  </action>
  <verify>`pnpm --filter @weatherchaser/web type-check` exits 0. `grep -n "setPointerCapture" apps/web/src/components/mobile/MobileBottomSheet.tsx` shows pointer capture. `grep -n "onSnapChange" apps/web/src/components/mobile/MobileBottomSheet.tsx` shows the callback fired on release. `grep -nE "100dvh|safe-area" apps/web/src/components/mobile/MobileBottomSheet.css` shows the dvh full-height + fallback. `grep -nE "glass-bg|color-border|shadow-glass" apps/web/src/components/mobile/MobileBottomSheet.css` confirms token usage. `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" apps/web/src/components/mobile/MobileBottomSheet.css | grep -v "rgba(0, *0, *0"` returns nothing (no hardcoded colors except the scrim rgba).</verify>
  <done>MobileBottomSheet.tsx is a controlled 3-snap sheet (snapIndex/onSnapChange) with handle-only pointer-drag using setPointerCapture, ref-driven transform during drag (no state churn), nearest-detent snap on release, tap-to-advance handle, scrim rendered only at full with pointer-events gated by detent, and an independently-scrolling content area (overscroll-behavior:contain) inside an overflow-visible wrapper. CSS uses 100dvh + safe-area fallback and design tokens only (scrim rgba excepted). Type-check passes.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @weatherchaser/web type-check` exits 0
2. `useIsMobile` returns boolean from `matchMedia('(max-width: 768px)')` and listens for changes
3. MobileBottomSheet drag is handle-bound with `setPointerCapture`, ref-driven transform during drag, `onSnapChange` only on release
4. Scrim renders only at full detent; `pointer-events:auto` only at full
5. CSS uses `100dvh` + safe-area fallback and design tokens (no hardcoded colors beyond the scrim rgba)
6. No new runtime dependency added (no vaul / react-modal-sheet / motion in package.json diff)
</verification>

<success_criteria>
A dependency-free `useIsMobile()` hook and a controlled three-snap `MobileBottomSheet` exist, type-check, render nothing in the app yet, and are ready for composition. The sheet drags at 60fps (ref transform during drag, state only on release), snaps to peek/half/full, shows a scrim only at full, scrolls its content independently of the drag gesture, and themes itself entirely through design tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-mobile-web-ux/03.1-01-SUMMARY.md`
</output>
