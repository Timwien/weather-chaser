---
phase: 03.1-mobile-web-ux
plan: "03"
type: execute
wave: 2
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - apps/web/src/routes/index.tsx
  - apps/web/src/components/map/MapContainer.tsx
  - apps/web/src/components/entry/EntryPanel.tsx
  - apps/web/src/styles/global.css
  - apps/web/src/components/entry/EntryPanel.css
  - apps/web/src/components/itinerary/ItineraryPanel.css
  - apps/web/src/components/finder/WeatherFinderPanel.css
  - apps/web/src/components/common/MobileTabBar.tsx
autonomous: false
requirements:
  - UI-MOBILE-COMPOSE-01
  - UI-MOBILE-MAPFIT-01
  - UI-MOBILE-CLEANUP-01

must_haves:
  truths:
    - "On a ≤768px viewport the app is map-first: the MapContainer fills the screen, the MobileSearchBar floats on top, and the panels (EntryPanel / ItineraryPanel / WeatherFinderPanel) render as children inside the MobileBottomSheet — there is NO MobileTabBar and NO Search/Map screen toggle"
    - "Ownership division is honored: the pill (Plan 02) is compact selection (mode toggle + 3 shared inputs); the LAUNCH step lives in EntryPanel, which is rendered AS-IS inside the bottom sheet in entry mode (EntryPanel still hosts useOptimizer + RouteConfigStep/WeatherFinderStep). The launch form appears exactly once — in the sheet, never in the pill."
    - "Auto-snap targets are pinned: in entry mode the sheet sits at PEEK (0); after a search (showResults || showFinderPanel becomes true) the sheet auto-rises to HALF (1); tapping a list item opens its detail by auto-snapping to FULL (2) in the SAME sheet (push/expand within the sheet), not a new screen"
    - "Tapping a map marker shows the existing mini map popup WITHOUT forcing the sheet to change snap (marker tap is decoupled from sheet snap on mobile)"
    - "The map auto-fits to the route AND finder results with extra bottom padding equal to the current sheet height (imported from getSheetHeights()), so markers — including finder rank-1 — sit ABOVE the sheet and are not hidden behind it; route fitBounds resets padding before each fit, and the finder flyTo passes the same bottom padding so rank-1 is not centered behind the sheet"
    - "When the sheet is pulled to full, the MobileSearchBar hides (animates out) so it does not compete with the sheet"
    - "Desktop (>768px) renders the existing screen-map + screen-panel layout completely unchanged; the old mobile @media full-screen overrides and the data-mobile-view / mobileView mechanism are removed"
    - "The peek strip shows a localized summary ('6 Orte gefunden' / 'Route: 5 Stopps · 480 km' / 'Tippen um zu suchen') using the mobile.sheet_summary_* keys from Plan 02"
    - "Sheet detent heights and the map's bottom padding come from the SAME getSheetHeights() helper exported by Plan 01 — Plan 03 does NOT recompute its own [84, innerHeight*0.5, innerHeight] array"
  artifacts:
    - path: "apps/web/src/routes/index.tsx"
      provides: "useIsMobile branch: mobile = MapContainer + MobileSearchBar + MobileBottomSheet(panels incl. EntryPanel); desktop = unchanged screen-map/screen-panel; sheetSnap state replaces mobileView; heights from imported getSheetHeights(); MobileTabBar removed"
      contains: "useIsMobile"
    - path: "apps/web/src/components/map/MapContainer.tsx"
      provides: "FitRouteOnSelection + FlyToFinderRank1 accept sheetBottomPadding; route fit resets padding before fitBounds; finder flyTo passes bottom padding; marker-tap decoupled from sheet snap"
      contains: "sheetBottomPadding"
    - path: "apps/web/src/components/entry/EntryPanel.tsx"
      provides: "Works correctly as bottom-sheet content (the launch step owner) on mobile; only minimal changes if needed for sheet hosting — useOptimizer + RouteConfigStep/WeatherFinderStep stay here"
      contains: "useOptimizer"
  key_links:
    - from: "apps/web/src/routes/index.tsx"
      to: "apps/web/src/components/mobile/MobileBottomSheet.tsx"
      via: "renders panels (incl. EntryPanel) as children; owns sheetSnap state + onSnapChange; imports getSheetHeights() for the snap→px mapping"
      pattern: "MobileBottomSheet|getSheetHeights"
    - from: "apps/web/src/routes/index.tsx"
      to: "apps/web/src/components/map/MapContainer.tsx"
      via: "passes sheetBottomPadding = getSheetHeights()[sheetSnap] so fitBounds/flyTo pad above the sheet"
      pattern: "sheetBottomPadding"
    - from: "apps/web/src/components/map/MapContainer.tsx"
      to: "maplibre setPadding/fitBounds + flyTo padding"
      via: "route: setPadding reset then fitBounds({ padding: { bottom: sheetBottomPadding + 60, ... } }); finder: flyTo({ ..., padding: { bottom: sheetBottomPadding + 60 } })"
      pattern: "setPadding"
    - from: "apps/web/src/routes/index.tsx"
      to: "apps/web/src/components/mobile/MobileSearchBar.tsx"
      via: "hidden={isMobile && sheetSnap === 2} hides the bar at full sheet"
      pattern: "hidden="
---

<objective>
Compose the Phase 3.1 mobile experience: wire `useIsMobile` (Plan 01), `MobileBottomSheet` (Plan 01), and `MobileSearchBar` (Plan 02) into `routes/index.tsx` behind a mobile branch; teach `MapContainer` to fit BOTH the route and the finder rank-1 ABOVE the sheet; decouple marker taps from sheet snap; strip the old two-screen mobile mechanism; and delete `MobileTabBar`. Desktop stays byte-for-byte unchanged.

**Ownership division (checker BLOCKER — the counterpart to Plan 02):** The pill (Plan 02) is the compact selection affordance (mode toggle + 3 shared inputs). The LAUNCH step stays in `EntryPanel`, which THIS plan renders as the bottom-sheet content in entry mode. `EntryPanel` keeps hosting `useOptimizer()` + `RouteConfigStep`/`WeatherFinderStep` exactly as today — we do NOT move those into the pill, and we render `EntryPanel` (not a re-built form) inside the sheet so the launch form exists in exactly one place. `EntryPanel.tsx` is in `files_modified` only for minimal changes needed to host it well inside the sheet (e.g. dropping its own footer/header chrome on mobile if it conflicts with the sheet) — keep changes minimal and desktop-identical; if no change is needed, leave it untouched and note that in the SUMMARY.

**Snap-height source of truth (checker WARNING 4):** Import `getSheetHeights()` from `MobileBottomSheet` (Plan 01 exports it) and use it for the snap→px mapping that feeds map padding. Do NOT recompute a local `[84, innerHeight*0.5, innerHeight]` array — the sheet and the map padding must read the same numbers.

This is the integration + cleanup plan and ends with a human device-verification checkpoint (real phone / device-mode) because the drag gesture, 100dvh, scrim, and map-padding feel cannot be verified by type-check alone — hence `autonomous: false`.

Purpose: turn the two primitives into the locked map-first flow (compact selection in pill → EntryPanel launch step in sheet → sheet rises with results → tap item opens detail in same sheet; marker tap = map popup only; map auto-fits route AND finder with bottom padding; search bar hides at full).
Output: an updated `index.tsx` mobile composition, an extended `MapContainer` (route + finder padding), minimal-if-any `EntryPanel` sheet-hosting tweaks, cleaned CSS across 4 files, a deleted `MobileTabBar.tsx`, and a passed device-verification checkpoint.
</objective>

<execution_context>
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timwienbokerd1pmist1tzsvp94/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-mobile-web-ux/03.1-CONTEXT.md
@.planning/phases/03.1-mobile-web-ux/03.1-RESEARCH.md
@.planning/phases/03.1-mobile-web-ux/03.1-01-SUMMARY.md
@.planning/phases/03.1-mobile-web-ux/03.1-02-SUMMARY.md
@apps/web/src/routes/index.tsx
@apps/web/src/components/map/MapContainer.tsx
@apps/web/src/components/map/StopMarkers.tsx
@apps/web/src/components/map/FinderMarkers.tsx
@apps/web/src/components/entry/EntryPanel.tsx
@apps/web/src/components/itinerary/ItineraryPanel.tsx
@apps/web/src/components/finder/WeatherFinderPanel.tsx
@apps/web/src/components/mobile/MobileBottomSheet.tsx
@apps/web/src/styles/global.css
@apps/web/src/components/entry/EntryPanel.css
@apps/web/src/components/itinerary/ItineraryPanel.css
@apps/web/src/components/finder/WeatherFinderPanel.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map auto-fit with sheet padding (route AND finder) + marker-tap decoupling</name>
  <files>apps/web/src/components/map/MapContainer.tsx</files>
  <action>
Make the map fit ABOVE the sheet for BOTH route and finder, and stop marker taps from yanking the sheet.

**1a. Thread `sheetBottomPadding` through `MapContainer`.** Add an optional prop `sheetBottomPadding?: number` (pixels of sheet currently covering the bottom; 0 on desktop). Pass it into `FitRouteOnSelection` AND into `FlyToFinderRank1`.

**1b. Fix `FitRouteOnSelection` (lines ~49–69).** Before calling `fitBounds`, reset accumulated padding (RESEARCH Pitfall 1 / MapLibre issue #4095): `map.getMap().setPadding({ top: 0, right: 0, bottom: 0, left: 0 });`. Then replace the uniform `padding: 60` with directional padding that reserves the sheet area:
```typescript
const bp = sheetBottomPadding ?? 0;
map.getMap().setPadding({ top: 0, right: 0, bottom: 0, left: 0 });
map.getMap().fitBounds(
  [[Math.min(...lngs), Math.min(...lats)], [Math.max(...lngs), Math.max(...lats)]],
  { padding: { top: 60, right: 60, left: 60, bottom: bp + 60 }, maxZoom: 9, duration: 600 },
);
```
Add `sheetBottomPadding` to the effect deps so a snap change re-fits. Keep desktop behaviour identical: when `sheetBottomPadding` is undefined/0 the result is effectively the old uniform 60 padding.

**1c. Fix `FlyToFinderRank1` (lines ~75–89) — finder padding is REQUIRED, not deferred (checker WARNING 3).** The locked decision applies auto-fit-with-sheet-padding to BOTH modes. `flyTo` accepts a `padding` option, so pass the same bottom offset so rank-1 is centered in the VISIBLE area above the sheet instead of behind it:
```typescript
const bp = sheetBottomPadding ?? 0;
map.getMap().flyTo({
  center: [rank1.lng, rank1.lat],
  zoom: 7,
  duration: 800,
  padding: { top: 0, right: 0, left: 0, bottom: bp },
});
```
Add `sheetBottomPadding` to the effect deps. On desktop (`bp` 0) this is identical to the current centered flyTo. (If MapLibre's `flyTo` padding does not visibly shift the center in testing, fall back to offsetting the center latitude by the equivalent screen pixels via `map.getMap().project`/`unproject` so rank-1 still clears the sheet — but try the `padding` option first; it is supported.) Do NOT leave finder padding deferred.

**1d. Decouple marker tap from sheet snap (RESEARCH "Map Marker Mini-Popup" + Open Question 2).** The popup already works — `StopMarkers`/`FinderMarkers` own their internal `popupIndex` and show the mini card on click. The coupling to remove is the SHEET reaction: on mobile a marker tap must NOT auto-scroll the sheet list or change snap. `MapContainer` does not own the sheet, so the decoupling is enforced at the callback layer: `MapContainer` accepts `onStopClick`/`onFinderClick` that, on mobile, the parent (Task 2) wires to a popup-only handler with NO `setSheetSnap`. The work here in `MapContainer` is to ensure the marker `onClick` only drives the popup + the existing fly behavior and introduces NO new sheet-snap side effect. Confirm StopMarkers/FinderMarkers already setPopupIndex internally (they do) — no change needed there. Document that the sheet-snap decoupling is completed by the index.tsx callback wiring in Task 2.

No user-facing strings added here. No design tokens involved (map logic only).
  </action>
  <verify>`pnpm --filter @weatherchaser/web type-check` exits 0. `grep -n "sheetBottomPadding" apps/web/src/components/map/MapContainer.tsx` shows the prop threaded into BOTH FitRouteOnSelection and FlyToFinderRank1. `grep -n "setPadding" apps/web/src/components/map/MapContainer.tsx` shows the route reset-before-fitBounds. `grep -n "padding" apps/web/src/components/map/MapContainer.tsx` shows the flyTo passing a `padding: { bottom: ... }` option for the finder. `grep -n "bottom: " apps/web/src/components/map/MapContainer.tsx` shows the sheet bottom offset in both paths.</verify>
  <done>MapContainer accepts sheetBottomPadding; FitRouteOnSelection resets padding then fits with { bottom: sheetBottomPadding + 60 }; FlyToFinderRank1 passes padding.bottom = sheetBottomPadding so finder rank-1 clears the sheet (not deferred). Both re-run on snap change. Desktop (no/zero padding) behaves identically to before. Marker taps drive only the existing popup/fly, with no new sheet-snap side effect. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: index.tsx mobile composition (EntryPanel in sheet) + CSS cleanup + delete MobileTabBar</name>
  <files>apps/web/src/routes/index.tsx
apps/web/src/components/entry/EntryPanel.tsx
apps/web/src/styles/global.css
apps/web/src/components/entry/EntryPanel.css
apps/web/src/components/itinerary/ItineraryPanel.css
apps/web/src/components/finder/WeatherFinderPanel.css
apps/web/src/components/common/MobileTabBar.tsx</files>
  <action>
**2a. Rewrite the render of `routes/index.tsx` with a mobile branch (RESEARCH "Mobile Layout Composition").** Keep ALL existing state/handlers (selectedStopIndex, selectedFinderIndex, computedFinderResults, flyToCity, showResults/showFinderPanel/showEntryPanel). Then:
- Remove `mobileView` state + `setMobileView`, the `data-mobile-view` attribute, and the `MobileTabBar` import + render.
- Add `const isMobile = useIsMobile();` and `const [sheetSnap, setSheetSnap] = useState<0|1|2>(0);`.
- **Snap heights come from Plan 01's exported helper (checker WARNING 4):** `import { MobileBottomSheet, getSheetHeights } from '../components/mobile/MobileBottomSheet';` then `const sheetPx = getSheetHeights()[sheetSnap];`. Do NOT recompute a local `[84, innerHeight*0.5, innerHeight]` array — use the single source of truth so map padding equals the sheet's real coverage.
- **Pinned auto-snap targets (checker INFO 5 — must be exact so the human verifier has clear pass/fail):**
  - Entry mode (no results yet): sheet at **PEEK (0)**.
  - After a search — `showResults || showFinderPanel` transitions to true: auto-rise to **HALF (1)**. Use a `useEffect` keyed on `[showResults, showFinderPanel]` that calls `setSheetSnap(1)` when either becomes true (mobile only).
  - Tapping a list item (detail) — the panel's `onStopSelect`/`onResultSelect`: auto-snap to **FULL (2)** in the same sheet (mobile only), in addition to the existing in-panel scroll/select.
  - Map-marker tap: **no snap change** (see 2a marker-tap split below).
  Guard all snap nudges with `if (isMobile)`.
- Marker-tap decoupling (Task 1d): on mobile, the `onStopClick`/`onFinderClick` passed to `MapContainer` update selection + popup/fly but must NOT call `setSheetSnap`. So split: list-item callbacks (from the panels) may change snap (to FULL); map-marker callbacks (to MapContainer) must not. Concretely keep `onFinderClick`/`onStopClick` (map) free of `setSheetSnap`, and only add `setSheetSnap(2)` inside the panel `onResultSelect`/`onStopSelect` handlers.

Mobile branch render (EntryPanel is the launch step inside the sheet — see ownership division):
```tsx
if (isMobile) {
  return (
    <div style={{ position: 'relative', width: '100%', height: '100%' }}>
      <MapContainer
        {...mapProps}
        sheetBottomPadding={sheetPx}
      />
      <MobileSearchBar
        isExpanded={searchExpanded}
        onExpandedChange={setSearchExpanded}
        hidden={sheetSnap === 2}
      />
      <MobileBottomSheet
        snapIndex={sheetSnap}
        onSnapChange={setSheetSnap}
        summary={<PeekSummary />}
      >
        <div className="mobile-sheet-content">
          {showEntryPanel && <EntryPanel />}
          {showResults && <ItineraryPanel selectedStopIndex={selectedStopIndex} onStopSelect={...} />}
          {showFinderPanel && <WeatherFinderPanel .../>}
        </div>
      </MobileBottomSheet>
      <LoadingOverlay />
    </div>
  );
}
```
- **EntryPanel is rendered as-is** (it owns the launch step + the hoisted `useOptimizer`); the pill does NOT duplicate it. This is the other half of the Plan 02 ownership boundary.
- Add `const [searchExpanded, setSearchExpanded] = useState(false);`.
- `PeekSummary` is a tiny inline element choosing the localized summary: `showFinderPanel` → `t('mobile.sheet_summary_finder', { count: computedFinderResults.length })`; `showResults` → `t('mobile.sheet_summary_route', { stops: route.stops.length, km: Math.round(totalKm) })` where `totalKm = sum of stop.distanceToNextKm`; else `t('mobile.sheet_summary_empty')`. Read `route` from the store (already imported via useAppStore — add `route` to the destructure if not present).
- Wrap panels in `<div className="mobile-sheet-content">` so the new mobile CSS can scope panel position-resets to that ancestor (RESEARCH Pitfall 3).

**2a-bis. EntryPanel sheet-hosting check (minimal).** Render EntryPanel inside the sheet and confirm it works as sheet content. EntryPanel keeps `useOptimizer` + `RouteConfigStep`/`WeatherFinderStep`. ONLY if its own chrome conflicts with the sheet (e.g. its fixed-position desktop wrapper, header, or footer overlapping the handle) add a minimal, mobile-scoped tweak — preferably via the `.mobile-sheet-content .entry-panel` CSS reset in 2c rather than touching the component. If a component change is unavoidable (e.g. conditionally hiding the desktop header/footer when inside the sheet), keep it minimal and desktop-identical, and document it in the SUMMARY. If no change is needed, leave EntryPanel.tsx untouched (it's listed in files_modified only to authorize a minimal tweak).

**Desktop branch:** keep the EXISTING return exactly (the `screen-map` + `screen-panel` structure) but WITHOUT the `data-mobile-view` attribute (it's gone). Everything else desktop stays byte-for-byte.

**2b. CSS cleanup — strip the dead mobile mechanism (RESEARCH "What Must Be Removed"):**
- `apps/web/src/styles/global.css`: delete the entire `@media (max-width: 768px)` block (the `[data-mobile-view="map"] .screen-panel` / `[data-mobile-view="itinerary"] .screen-map` rules, ~lines 36–50).
- `apps/web/src/components/entry/EntryPanel.css`: delete the `@media (max-width: 768px)` full-screen `.entry-panel` block (~lines 1176–1186).
- `apps/web/src/components/itinerary/ItineraryPanel.css`: delete the `@media (max-width: 768px)` `.itinerary-panel` full-screen block AND the stale `.mobile-screen-map-active .itinerary-panel` rule (~lines 148–168).
- `apps/web/src/components/finder/WeatherFinderPanel.css`: delete the `@media (max-width: 768px)` `.weather-finder-panel` full-screen block AND the stale `.mobile-screen-map-active .weather-finder-panel` rule (~lines 105–124).

**2c. CSS additions — panels render correctly inside the sheet (RESEARCH Pattern 4 + Pitfall 3).** Add a new `@media (max-width: 768px)` block (in `global.css`, or per-panel CSS — keep scoped to `.mobile-sheet-content`) that, ONLY under `.mobile-sheet-content`, resets the panels' desktop `position: fixed` to static and width/height to fluid:
```css
@media (max-width: 768px) {
  .mobile-sheet-content .entry-panel,
  .mobile-sheet-content .itinerary-panel,
  .mobile-sheet-content .weather-finder-panel {
    position: static;
    inset: auto;
    width: 100%;
    height: auto;
    max-height: none;
    border-radius: 0;
    box-shadow: none;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    background: transparent; /* sheet provides the glass surface */
    padding: var(--space-4);
    z-index: auto;
  }
}
```
NEVER modify the base (desktop) panel rules — only add scoped overrides under `.mobile-sheet-content`. Use design tokens for any values; no hardcoded colors.

**2d. Delete `MobileTabBar.tsx`** (`apps/web/src/components/common/MobileTabBar.tsx`) — the component is fully removed. Confirm no other importers: `grep -rn "MobileTabBar" apps/web/src` returns nothing after the index.tsx import is removed.

i18n: the peek summary strings use `t('mobile.sheet_summary_*')` from Plan 02 — no hardcoded strings. Any new aria-label uses t(). Design-token + desktop-untouched rules apply throughout.
  </action>
  <verify>`pnpm --filter @weatherchaser/web type-check` exits 0. `grep -rn "MobileTabBar" apps/web/src` returns nothing. `grep -rn "data-mobile-view\|mobileView" apps/web/src` returns nothing. `grep -rn "mobile-screen-map-active" apps/web/src` returns nothing. `grep -n "useIsMobile\|MobileBottomSheet\|MobileSearchBar\|sheetSnap" apps/web/src/routes/index.tsx` shows the composition. `grep -n "getSheetHeights" apps/web/src/routes/index.tsx` shows the snap heights imported from Plan 01 (no local [84, ...] array — `grep -n "innerHeight \* 0.5" apps/web/src/routes/index.tsx` returns nothing). `grep -n "setSheetSnap(1)" apps/web/src/routes/index.tsx` shows auto-rise-to-half; `grep -n "setSheetSnap(2)" apps/web/src/routes/index.tsx` shows detail-to-full. `grep -n "EntryPanel" apps/web/src/routes/index.tsx` shows EntryPanel rendered inside the sheet. `grep -n "sheet_summary" apps/web/src/routes/index.tsx` shows the localized peek summary. `grep -n "mobile-sheet-content" apps/web/src/styles/global.css` (or the panel CSS files) shows the scoped position-reset. `pnpm --filter @weatherchaser/web build` succeeds.</verify>
  <done>index.tsx renders a useIsMobile mobile branch (Map + MobileSearchBar + MobileBottomSheet wrapping the panels — incl. EntryPanel as the launch step — in .mobile-sheet-content) with sheetSnap state, snap heights imported from getSheetHeights(), pinned auto-snaps (entry=peek, search=half, detail=full, marker=no-change), in-sheet detail, search-bar-hidden-at-full, and a localized peek summary; the desktop branch is unchanged sans the removed data-mobile-view attr. EntryPanel hosts as sheet content with minimal/no change. All old mobile @media full-screen blocks + stale selectors are deleted; scoped .mobile-sheet-content position-resets added with tokens only. MobileTabBar.tsx is deleted with no remaining references. Type-check and build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification of the map-first mobile layout</name>
  <files>apps/web/src/routes/index.tsx</files>
  <action>
This is a human verification gate — no code is written in this task. Claude has already automated the full implementation in Tasks 1–2. Run the dev server and present the verification steps below to the user, then pause for their approval.
  </action>
  <what-built>
The full map-first mobile layout: a permanently-visible map, a floating Airbnb-style compact search bar (compact selection only), and a three-snap draggable bottom sheet hosting the entry/itinerary/finder panels — with the EntryPanel launch step living in the sheet, MobileTabBar removed, pinned auto-snaps (entry=peek, search=half, detail=full), in-sheet detail, marker-popup decoupling, search-bar hide-at-full, and sheet-aware map fitting for BOTH route and finder. Dark mode and EN/DE i18n applied throughout. Desktop layout untouched.
  </what-built>
  <how-to-verify>
Run `pnpm --filter @weatherchaser/web dev` and open the app, then use browser device-mode (or a real phone) at a ≤768px viewport. Verify:
1. Map fills the screen; the compact search pill floats at the top showing location · dates · a criterion icon; there is NO bottom Search/Map tab bar.
2. Tap the pill → it expands inline at the top into a COMPACT form: a Plan route / Find weather toggle and the date/location/criteria inputs ONLY (the generate/launch step is NOT duplicated in the pill — it appears in the sheet). The date calendar popover is NOT clipped. Close collapses it.
3. In entry mode the sheet sits at PEEK and shows the EntryPanel launch step when expanded; run a search → the sheet auto-rises to HALF and the peek strip shows a localized summary (e.g. "6 Orte gefunden" / "Route: 5 Stopps · 480 km"). Switch language to German and confirm the summary + toggle labels are German.
4. Drag the sheet handle through peek → half → full smoothly (no jank); tapping the handle advances detents. At FULL, a light scrim dims the map and the search bar hides (slides up). At peek/half the map stays bright and pannable.
5. Tap a list item in the sheet → its detail opens in the SAME sheet and the sheet auto-snaps to FULL (no new screen). Tap a map marker → the mini popup appears on the map and the sheet does NOT change snap.
6. The map auto-fits so all markers sit ABOVE the sheet (none hidden behind it) for BOTH a route and a finder search — in finder mode, rank-1 is centered in the visible area above the sheet, not behind it; changing the sheet height re-fits with correct padding (no double-offset).
7. Toggle dark mode (Settings) → sheet, pill, handle, scrim, and panels all adapt via tokens (no light/odd patches).
8. Resize the browser above 768px → the original DESKTOP layout returns unchanged (left panel + full map, no sheet, no pill).
  </how-to-verify>
  <verify>All 8 verification steps pass on a ≤768px viewport, and the desktop layout is unchanged above 768px.</verify>
  <done>The user has confirmed all 8 checks pass (or reported issues that were then fixed and re-verified).</done>
  <resume-signal>Type "approved" if all 8 checks pass, or describe the issues (e.g. "drag janky at full", "summary not translating", "finder rank-1 behind sheet", "markers hidden behind sheet") and they will be fixed.</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @weatherchaser/web type-check` exits 0 and `build` succeeds
2. No remaining references: `grep -rn "MobileTabBar\|data-mobile-view\|mobileView\|mobile-screen-map-active" apps/web/src` is empty
3. index.tsx has a useIsMobile mobile branch composing Map + MobileSearchBar + MobileBottomSheet(panels incl. EntryPanel in .mobile-sheet-content); desktop branch unchanged
4. Snap heights imported from getSheetHeights() (Plan 01) — no local [84, innerHeight*0.5, innerHeight] recomputation
5. Auto-snaps pinned: entry=peek(0), search=half(1), list-item-detail=full(2), marker-tap=no change
6. MapContainer fits route with reset-then-directional padding ({ bottom: sheetBottomPadding + 60 }) AND finder flyTo passes padding.bottom = sheetBottomPadding (finder not deferred); marker tap does not change sheet snap
7. Peek summary + toggle use mobile.* i18n keys (EN/DE); CSS token-driven; .mobile-sheet-content position-resets scoped (base panel rules untouched)
8. Human device-verification checkpoint passed (all 8 checks)
</verification>

<success_criteria>
On ≤768px the app is the locked map-first experience: floating compact search bar (compact selection) + three-snap draggable bottom sheet (hosting the EntryPanel launch step + results/detail) over a permanent map, no tab bar, pinned auto-snaps (entry=peek, search=half, detail=full), in-sheet detail, marker popups decoupled from the sheet, sheet-aware map fitting for BOTH route and finder (rank-1 above the sheet), search bar hiding at full, snap heights from the single getSheetHeights() source of truth, and a localized peek summary — all dark-mode-correct via tokens and fully EN/DE i18n'd. Desktop is byte-for-byte unchanged. Type-check + build pass and the human device-verification checkpoint is approved.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-mobile-web-ux/03.1-03-SUMMARY.md`
</output>
</content>
</invoke>
