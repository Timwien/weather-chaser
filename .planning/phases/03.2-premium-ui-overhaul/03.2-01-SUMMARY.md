---
phase: 03.2-premium-ui-overhaul
plan: "01"
subsystem: ui
tags: [dark-mode, theme, zustand, css-variables, i18n, react-i18next, accessibility, glassmorphism]

# Dependency graph
requires:
  - phase: 03-04
    provides: SettingsTab with language switcher (theme control added alongside)
  - phase: 01-04
    provides: design-token system (tokens.css) and react-i18next setup that dark mode + i18n build on
provides:
  - Auto dark mode (system/light/dark) with OS-preference default, persisted override, live OS sync, no-flash load
  - Full i18n enforcement for previously hardcoded UI strings (a11y attributes + visible German text)

affects:
  - 03.1
  - premium-ui
  - any-future-ui-work

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Theme = [data-theme] attribute on <html> + single [data-theme=dark] token override block; every token-driven component adapts automatically"
    - "Pre-paint inline script in index.html reads localStorage before first paint to prevent theme flash"
    - "Plain zustand store + direct localStorage (key wc-theme), not persist middleware — matches existing one-time-hint pattern; single source of truth shared with the inline script"
    - "initTheme() returns cleanup, wired in main.tsx Root useEffect alongside auth init; matchMedia listener only re-applies while mode === 'system'"
    - "a11y i18n namespace for accessibility/tooltip attribute strings"

key-files:
  created:
    - apps/web/src/stores/themeStore.ts
  modified:
    - apps/web/index.html
    - apps/web/src/main.tsx
    - apps/web/src/styles/tokens.css
    - apps/web/src/styles/global.css
    - apps/web/src/components/account/SettingsTab.tsx
    - apps/web/src/i18n/locales/en/common.json
    - apps/web/src/i18n/locales/de/common.json
    - apps/web/src/components/entry/EntryPanel.tsx
    - apps/web/src/components/entry/DateRangePicker.tsx
    - apps/web/src/components/entry/RouteConfigStep.tsx
    - apps/web/src/components/account/SavedTab.tsx

key-decisions:
  - "Plan re-scoped before execution: typography, glassmorphism, micro-animations were already delivered ad-hoc in earlier commits (Plus Jakarta Sans, --glass-* tokens, transitions). This plan built only the two genuinely-missing parts: auto dark mode + i18n enforcement."
  - "Theme persisted to localStorage only — DB user-preferences sync deferred (no preferences table exists; would be backend work outside this UI phase)."
  - "Dark palette keeps secondary/tertiary and --score-* data-vis colors vivid/unchanged; only surfaces/text/borders/glass/shadows + a brighter primary teal are overridden."
  - "i18n scope grew during execution: an attribute-only audit had under-counted. SavedTab had 6 hardcoded German strings and DateRangePicker a hardcoded 'Clear'; all fixed. Brand names (Google/Apple) and language self-names (Deutsch/English) intentionally left untranslated."

patterns-established:
  - "Dark mode via design tokens: add a [data-theme=dark] block, never per-component dark styles"
  - "No-flash theming: pre-paint inline script + matching store source-of-truth key"

requirements-completed: [UI-DARK-01, UI-DARK-02, I18N-A11Y-01]

# Metrics
duration: ~25min
completed: 2026-06-01
---

# Phase 3.2 Plan 01: Premium UI Overhaul (Dark Mode + i18n) Summary

**Auto dark mode (OS-default, persisted override, live OS sync, no-flash) plus full i18n enforcement of previously hardcoded UI strings — the two parts of the premium overhaul that were never built.**

## Performance

- **Duration:** ~25 min (excludes a crashed first executor run; see Deviations)
- **Tasks:** 2 completed
- **Files modified:** 11 (1 created)
- **Type-check:** `pnpm --filter @weatherchaser/web type-check` → exit 0

## Accomplishments
- Auto dark mode: `themeStore.ts` (system/light/dark), `[data-theme="dark"]` token block, pre-paint no-flash script, Settings System/Light/Dark control, live OS tracking while in System mode
- i18n enforcement: EN/DE at strict parity (170/170 keys); zero hardcoded user-facing aria-label/title/placeholder/alt attributes
- Fixed additional hardcoded German visible text discovered during execution (SavedTab section titles + empty states, EntryPanel route hint, DateRangePicker "Clear")

## Task Commits

1. **Task 1: Auto dark mode + theme state** - `2352e67` (feat)
2. **Task 2: i18n enforcement** - `a5e7ff7` (i18n)

## Files Created/Modified
- `apps/web/src/stores/themeStore.ts` - **created** — theme store + initTheme() OS listener
- `apps/web/index.html` - pre-paint inline script (data-theme from wc-theme)
- `apps/web/src/main.tsx` - initTheme() wired with cleanup
- `apps/web/src/styles/tokens.css` - `[data-theme="dark"]` override block + `color-scheme`
- `apps/web/src/styles/global.css` - smooth background/color transition
- `apps/web/src/components/account/SettingsTab.tsx` - System/Light/Dark control
- `apps/web/src/i18n/locales/{en,de}/common.json` - theme + a11y + saved + clear keys (parity)
- `apps/web/src/components/entry/EntryPanel.tsx` - a11y attrs + route-hint text → keys
- `apps/web/src/components/entry/DateRangePicker.tsx` - a11y attrs + "Clear" → key
- `apps/web/src/components/entry/RouteConfigStep.tsx` - stepper a11y attrs → keys
- `apps/web/src/components/account/SavedTab.tsx` - section titles, empty states, a11y attrs → keys

## Deviations
- **First executor run crashed** (API ConnectionRefused) after completing Task 1's code but before committing or starting Task 2. Recovery: verified Task 1 output was correct (matched plan exactly), completed Task 2, then committed both tasks atomically.
- **i18n scope expanded:** the plan estimated ~11 hardcoded attribute strings. Execution found additional hardcoded German *visible* text (SavedTab × 6, EntryPanel route hint × 2, DateRangePicker "Clear" × 1) that an attribute-only audit had missed. All fixed; EN/DE parity maintained.

## Verification Notes (for human UAT)
- Dark mode live OS-sync, no-flash on reload, and Settings persistence are runtime behaviors best confirmed in the browser (`pnpm --filter @weatherchaser/web dev`), not by type-check alone.
